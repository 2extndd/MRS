# 💰 План оптимизации затрат Railway (4 бота)

## 📊 Текущая ситуация

**Проблема:** 4 бота на Railway стоят дорого

**Архитектура каждого бота:**
- Web процесс (gunicorn + Flask UI)
- Worker процесс (scheduler + scanner)
- PostgreSQL база данных
- ~36MB размер проекта

**Основные расходы Railway:**
1. **CPU time** - сканирование + API calls (~80%)
2. **Database operations** - PostgreSQL I/O (~15%)
3. **Memory** - постоянные процессы (~5%)
4. **Network** - egress traffic (минимально)

---

## 🎯 План оптимизации (от простого к сложному)

### 🟢 Уровень 1: Быстрые оптимизации (сделать сразу)

**Экономия:** ~30-40% от текущих расходов
**Сложность:** Низкая (2-3 часа работы)

#### 1.1. Увеличить интервалы сканирования

**Текущее:** 60-300 секунд (очень частые проверки)
**Оптимальное:** 5-10 минут (300-600 секунд)

```python
# configuration_values.py или через Web UI
SEARCH_INTERVAL = 600  # 10 минут вместо 1-5 минут
```

**Обоснование:**
- Новые товары на Mercari появляются не каждую минуту
- Проверка каждые 10 минут всё равно даёт быстрое реагирование
- Экономия CPU: ~50% меньше сканирований

**Экономия:** ~20-30% CPU time

---

#### 1.2. Уменьшить количество товаров на поиск

**Текущее:** 5-50 товаров на поиск
**Оптимальное:** 2-3 товара

```python
MAX_ITEMS_PER_SEARCH = 2  # вместо 5
```

**Обоснование:**
- После фикса blacklist каждый товар = 1 `full_item()` API call
- 2 товара вместо 5 = 60% меньше API calls
- Меньше загрузки изображений
- При частом сканировании 2 товара достаточно

**Экономия:** ~40% CPU time + ~40% memory

---

#### 1.3. Отключить автоматическую загрузку изображений

**Текущее:** Скачиваем и кодируем HIGH-RES изображения в base64
**Оптимальное:** Хранить только URL

```python
# core.py - закомментировать загрузку изображений
image_data = None  # Вместо download_and_encode_image()
```

**Telegram:** Отправлять по URL, а не по base64

**Обоснование:**
- Загрузка изображения = ~3-4 секунды + 100KB трафика на товар
- База данных растёт быстрее (base64 = 1.33x размер)
- Telegram может загружать по URL

**Экономия:** ~30% CPU time + ~50% DB storage + ~60% network egress

---

#### 1.4. Очистка старых логов

**Текущее:** Логи накапливаются бесконечно
**Оптимальное:** Удалять логи старше 7 дней

```sql
-- Добавить cronjob или background task
DELETE FROM logs WHERE created_at < NOW() - INTERVAL '7 days';
DELETE FROM items WHERE created_at < NOW() - INTERVAL '30 days';
```

**Экономия:** ~20% DB storage

---

### 🟡 Уровень 2: Средние оптимизации (сделать через неделю)

**Экономия:** дополнительно ~20-30%
**Сложность:** Средняя (1-2 дня работы)

#### 2.1. Один общий PostgreSQL для всех 4 ботов

**Текущее:** 4 отдельные базы = 4x стоимость
**Оптимальное:** 1 PostgreSQL, 4 схемы/префикса

```python
# Структура:
# bot1_items, bot1_searches, bot1_logs
# bot2_items, bot2_searches, bot2_logs
# bot3_items, bot3_searches, bot3_logs
# bot4_items, bot4_searches, bot4_logs
```

**Реализация:**
1. Создать 1 PostgreSQL на Railway
2. Добавить префикс таблиц для каждого бота
3. Изменить `DATABASE_URL` для каждого бота

**Экономия:** ~70% на PostgreSQL (платишь за 1 вместо 4)

---

#### 2.2. Использовать SQLite вместо PostgreSQL

**Для ботов с малым трафиком (< 30 поисков):**

```python
# .env
DATABASE_URL=sqlite:///mercari_bot.db
```

**Обоснование:**
- SQLite бесплатный (файл на диске)
- Достаточно для 1-2 ботов с низким QPS
- Railway включает 1GB disk бесплатно

**Ограничения:**
- Нет параллельных записей
- Меньше надёжность
- Подходит только для малых ботов

**Экономия:** ~$5-10/месяц на PostgreSQL

---

#### 2.3. Пакетные операции БД

**Текущее:** Каждый товар = 1 INSERT
**Оптимальное:** Батчи по 10-20 товаров

```python
# db.py - add_items_batch()
def add_items_batch(self, items):
    """Insert multiple items in one query"""
    query = "INSERT INTO items (...) VALUES (%s, %s, ...) ON CONFLICT DO NOTHING"
    self.execute_many(query, items)
```

**Экономия:** ~30% DB I/O operations

---

### 🔴 Уровень 3: Агрессивные оптимизации (если всё равно дорого)

**Экономия:** дополнительно ~30-50%
**Сложность:** Высокая (3-5 дней работы)

#### 3.1. Объединить все 4 бота в 1 приложение

**Архитектура:**
```
1 Railway проект:
  - 1 Web UI (управление всеми 4 ботами)
  - 1 Worker (сканирует для всех 4 ботов)
  - 1 PostgreSQL (общая база)
  
Таблицы:
  - bots (id, name, config)
  - searches (bot_id, query, url)
  - items (bot_id, search_id, ...)
```

**Преимущества:**
- Платишь за 1 проект вместо 4
- Общий scheduler эффективнее
- Один Web UI для всех
- Легче управлять

**Недостатки:**
- Все яйца в одной корзине
- Нужна миграция данных

**Экономия:** ~70-75% (платишь за 1 вместо 4)

---

#### 3.2. Перенос на более дешёвый хостинг

**Альтернативы Railway:**

1. **Render.com** (~30% дешевле)
   - Free tier: 512MB RAM бесплатно
   - PostgreSQL: бесплатно до 1GB
   
2. **Fly.io** (~40% дешевле)
   - Машины по требованию (не 24/7)
   - PostgreSQL: бесплатно до 3GB
   
3. **AWS Lambda + RDS** (~50% дешевле)
   - Serverless = платишь за выполнение
   - RDS PostgreSQL: t3.micro ~$15/месяц
   
4. **VPS (DigitalOcean, Hetzner)** (~70% дешевле)
   - $5-10/месяц = 1-2GB RAM
   - Ставишь PostgreSQL локально
   - Все 4 бота на 1 VPS

**Рекомендация:** VPS самый дешёвый для 4 ботов

**Экономия:** $50-150/месяц (в зависимости от текущих затрат)

---

#### 3.3. Кэширование результатов поиска

**Идея:** Не сканировать один и тот же поиск чаще 1 раза в 10 минут

```python
# В памяти или Redis
search_cache = {
    'search_url_hash': {
        'timestamp': datetime,
        'items': [...],
        'ttl': 600  # 10 минут
    }
}
```

**Обоснование:**
- Если несколько поисков похожи - используй кэш
- Mercari обновляется не каждую секунду

**Экономия:** ~20-30% API calls

---

## 📈 Итоговый план действий

### Фаза 1: Немедленно (сегодня-завтра)

✅ **Изменить настройки всех 4 ботов:**
```
SEARCH_INTERVAL = 600 (10 минут)
MAX_ITEMS_PER_SEARCH = 2
```

✅ **Отключить загрузку изображений** (хранить только URL)

✅ **Добавить очистку старых логов** (>7 дней)

**Ожидаемая экономия:** ~40-50% от текущих затрат

---

### Фаза 2: Через неделю

✅ **Объединить 4 PostgreSQL в 1 общую базу** (с префиксами)

✅ **Для малых ботов** - перейти на SQLite

✅ **Батчинг операций БД**

**Ожидаемая экономия:** дополнительно ~20-30%

---

### Фаза 3: Если всё равно дорого (через месяц)

✅ **Объединить 4 бота в 1 приложение**

ИЛИ

✅ **Переехать на VPS** (DigitalOcean $5-10/мес)

**Ожидаемая экономия:** дополнительно ~50-70%

---

## 💡 Дополнительные идеи

### A. Умное сканирование

- Сканировать активные поиски чаще (каждые 5 мин)
- Неактивные реже (каждые 30 мин)
- Определять активность по количеству новых товаров

### B. Приоритизация поисков

- Важные поиски = чаще + больше товаров
- Неважные = реже + меньше товаров
- UI для управления приоритетом

### C. Webhook-based уведомления

- Вместо постоянного сканирования
- Использовать Mercari webhooks (если доступны)
- Сканировать только при событии

---

## 📊 Примерная экономия

**Текущие затраты (предположение):**
- 4 бота на Railway: ~$40-80/месяц
- PostgreSQL x4: ~$20-40/месяц
- **Итого:** ~$60-120/месяц

**После Фазы 1:**
- Экономия: ~40-50%
- **Новая стоимость:** ~$36-60/месяц

**После Фазы 2:**
- Экономия: ~60-70%
- **Новая стоимость:** ~$18-36/месяц

**После Фазы 3:**
- Экономия: ~80-90%
- **Новая стоимость:** ~$6-12/месяц (VPS)

---

## 🎯 Моя рекомендация

**Начни с Фазы 1** (можно сделать за 1-2 часа):
1. Увеличь `SEARCH_INTERVAL` до 600 сек (10 мин)
2. Уменьши `MAX_ITEMS_PER_SEARCH` до 2
3. Отключи загрузку изображений (храни только URL)

Это даст **~40-50% экономии** без больших изменений кода!

Если этого недостаточно → Фаза 2 (объединить БД)

Если всё равно дорого → Фаза 3 (VPS за $5-10/мес)

---

**Хочешь, помогу реализовать какую-то фазу?** 🚀
