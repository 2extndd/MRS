# ⚡ Быстрая экономия Railway - 40-50% за 1 час!

## 🎯 Три простых изменения = большая экономия

### 1️⃣ Увеличить интервал сканирования (5 минут)

**Для каждого бота:**

```
1. Открой Web UI: https://your-bot.railway.app/config
2. Найди "Search Interval (seconds)"
3. Измени: 60 → 600 (10 минут вместо 1 минуты)
4. Сохрани
```

**Экономия:** ~30% CPU time

---

### 2️⃣ Уменьшить товаров на поиск (5 минут)

**Для каждого бота:**

```
1. В той же странице Config
2. Найди "Max Items Per Search"
3. Измени: 5 → 2
4. Сохрани
```

**Экономия:** ~40% CPU time + память

---

### 3️⃣ Отключить загрузку изображений (30 минут)

**Изменить код в `core.py` (строка ~420):**

```python
# БЫЛО:
image_data = download_and_encode_image(image_url, timeout=20, use_proxy=False)

# СТАЛО:
image_data = None  # Не загружаем изображения, только URL
```

**В `mercari_notifications.py` (строка ~100):**

```python
# БЫЛО:
if item_data.get('image_data'):
    bot.send_photo(chat_id, photo=item_data['image_data'], caption=message)

# СТАЛО:
if item.get('image_url'):
    bot.send_photo(chat_id, photo=item['image_url'], caption=message)
```

**Экономия:** ~30% CPU + ~50% DB storage + ~60% network

---

## 📊 Итого

**Время:** 30-60 минут на все 4 бота
**Экономия:** ~40-50% от текущих затрат

---

## 🔧 Как применить к 4 ботам?

### Вариант A: Вручную (1 час)
- Открыть каждый бот
- Изменить настройки через UI
- Для кода: изменить и deploy каждый бот

### Вариант B: Railway API (30 минут)
```bash
# Скрипт для обновления всех 4 ботов
for bot in bot1 bot2 bot3 bot4; do
  railway variables set SEARCH_INTERVAL=600 -s $bot
  railway variables set MAX_ITEMS_PER_SEARCH=2 -s $bot
done
```

---

## 💰 Ожидаемый результат

**Если сейчас платишь $80/месяц:**
- После изменений: ~$40-45/месяц
- **Экономия: ~$35-40/месяц**

**Если сейчас платишь $40/месяц:**
- После изменений: ~$20-25/месяц
- **Экономия: ~$15-20/месяц**

---

## ❓ FAQ

**Q: Не пропущу ли я новые товары?**
A: Нет! 10 минут - всё равно быстро. Товары на Mercari не появляются каждую секунду.

**Q: Почему только 2 товара на поиск?**
A: При интервале 10 минут + сортировке по времени - 2 товара достаточно для отслеживания новых.

**Q: Что с изображениями в Telegram?**
A: Telegram загрузит по URL. Качество то же, но не храним в БД.

---

**Готов начать?** Начни с изменения настроек через Web UI - это займёт 5 минут на бот! 🚀
