# 🤖 AI AGENT MEMORY - MercariSearcher Bot

## ⚠️ ПОСТОЯННЫЕ ПРАВИЛА

### Правило 1: Документация
- **НЕ создавать** множество файлов документации
- **Использовать ТОЛЬКО** этот файл `AI_AGENT_MEMORY.md`
- Все инструкции, изменения, контекст - ТОЛЬКО ЗДЕСЬ
- **НЕ тратить токены** на бесполезную документацию

### Правило 2: Команда "ЗАПОМНИ!"
- После слова "ЗАПОМНИ!" добавлять постоянные правила в этот файл
- Правила действуют для всех будущих сессий

### Правило 3: Команда "ЗАПОМНИ КОНТЕКСТ!"
- Сохранять последний контекст работы в этот файл
- Для использования другими AI агентами

---

## 📋 О ПРОЕКТЕ: MercariSearcher Bot

### Что это?
Telegram бот для мониторинга Mercari.jp (японский маркетплейс). Автоматически:
- Ищет товары по ключевым словам
- Скачивает фото в ВЫСОКОМ разрешении
- Сохраняет в PostgreSQL
- Отправляет уведомления в Telegram

### Основа
- **Язык**: Python 3
- **API библиотека**: `mercapi` (v0.4.2+)
- **База данных**: PostgreSQL на Railway
- **Платформа**: Railway (web + worker)
- **Telegram**: python-telegram-bot

### Архитектура

```
mercari_notifications.py (main)
  ├─> core.py (MercariSearcher) - поиск товаров
  │   └─> pyMercariAPI/mercari.py - обёртка вокруг mercapi
  │       └─> mercapi library - реальный API Mercari
  │
  ├─> image_handler.py - загрузка HIGH-RES фото
  │
  ├─> db.py - работа с PostgreSQL
  │
  └─> simple_telegram_worker.py - отправка в Telegram
```

### Полный цикл работы

1. **Планировщик** запускается каждые N секунд
2. **Поиск** через `mercapi.search(query="keyword")`
3. **Обработка каждого товара**:
   - Получить детали: `mercapi.item(item_id)`
   - Конвертировать URL фото в HIGH-RES (`/item/detail/orig/photos/`)
   - Скачать фото (БЕЗ прокси!)
   - Закодировать в base64
   - Сохранить в PostgreSQL
4. **Фильтрация** дубликатов (по mercari_id)
5. **Telegram** уведомления с high-res фото

---

## 🔧 КЛЮЧЕВЫЕ ФАЙЛЫ

### Основные модули
- `mercari_notifications.py` - точка входа, планировщик
- `core.py` - класс MercariSearcher, вся логика поиска
- `image_handler.py` - загрузка HIGH-RES изображений ⭐
- `db.py` - работа с PostgreSQL
- `simple_telegram_worker.py` - Telegram уведомления
- `configuration_values.py` - конфигурация

### API обёртка
- `pyMercariAPI/mercari.py` - синхронный wrapper вокруг mercapi
- `pyMercariAPI/items.py` - классы Item/Items
- `pyMercariAPI/exceptions.py` - исключения

### Тесты
- `test_real_mercari.py` - работающий тест
- `railway_test_deployment.py` - тест для Railway

---

## ⭐ ПОСЛЕДНИЕ ИЗМЕНЕНИЯ (19 ноября 2024)

### 1. Создан `image_handler.py`
**Функции**:
- `get_original_image_url(url)` - конвертирует любой URL в HIGH-RES
  - Входной: `https://static.mercdn.net/thumb/...` (240px)
  - Выходной: `https://static.mercdn.net/item/detail/orig/photos/mXXX_N.jpg` (1200px+)
- `download_and_encode_image(url)` - скачивает и кодирует в base64
  - БЕЗ прокси! Mercari CDN не блокирует
  - Возвращает: `data:image/jpeg;base64,...`

### 2. Исправлен `core.py`
**Метод `_process_new_items()`**:
- Исправлен доступ к атрибутам Item: `.id`, `.title`, `.price`
- Добавлена конвертация URL в HIGH-RES
- Добавлена загрузка и кодирование фото
- Сохранение `image_data` в базу

### 3. Исправлен `pyMercariAPI/mercari.py`
**Метод `search()`**:
- Было: `await m.search(keyword=...)`
- Стало: `await m.search(query=...)`
- Фикс: mercapi использует `query`, не `keyword`

### 4. Исправлен `simple_telegram_worker.py`
**Метод `send_item_notification()`**:
- Добавлена конвертация URL в HIGH-RES перед отправкой
- Telegram получает оригинальное качество

### 5. Исправлен `test_real_mercari.py`
- Обновлён синтаксис: `query=` вместо `keyword=`

---

## 📊 БАЗА ДАННЫХ (PostgreSQL)

### Таблица `items`
```sql
id              SERIAL PRIMARY KEY
mercari_id      VARCHAR(50) UNIQUE
title           TEXT
price           INTEGER
image_url       TEXT              -- HIGH-RES URL
image_data      TEXT              -- Base64 encoded image ⭐
telegram_sent   BOOLEAN
created_at      TIMESTAMP
```

**Важно**: 
- `image_url` - URL в высоком разрешении
- `image_data` - base64 закодированное фото (130-500KB)
- Все новые товары сохраняются с фото!

---

## 🚀 RAILWAY

### Project ID
`f17da572-14c9-47b5-a9f1-1b6d5b6dea2d`

### Переменные окружения
```bash
DATABASE_URL=postgresql://...
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHAT_ID=...
TELEGRAM_THREAD_ID=...  # опционально
PROXY_ENABLED=false     # фото работают БЕЗ прокси!
```

### Деплой
```bash
railway link -p f17da572-14c9-47b5-a9f1-1b6d5b6dea2d
git add .
git commit -m "commit message"
git push
railway logs -f
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Локально
```bash
python3 test_real_mercari.py
```

### На Railway
```bash
railway ssh
python3 railway_test_deployment.py
```

### Последние результаты (19 ноября 2024)
- ✅ Найдено: 120 товаров
- ✅ Добавлено: 70 НОВЫХ с high-res фото
- ✅ Отфильтровано: 50 дубликатов
- ✅ Успешность: 100%

---

## 🔍 ВАЖНЫЕ ДЕТАЛИ

### mercapi синтаксис
```python
# ПРАВИЛЬНО:
await m.search(query="keyword")
item = await m.item("m12345678")

# НЕПРАВИЛЬНО (старый код):
await m.search(keyword="keyword")  # ❌
```

### Item атрибуты (mercapi)
```python
item.id_           # ID товара
item.name          # название
item.price         # цена
item.photos        # список ORIGINAL URLs
item.thumbnails    # список thumbnail URLs
item.item_condition.name   # состояние
item.item_category.name    # категория
```

### HIGH-RES изображения
```python
from image_handler import get_original_image_url, download_and_encode_image

# Конвертация URL
high_res_url = get_original_image_url(thumbnail_url)

# Загрузка и кодирование
image_data = download_and_encode_image(high_res_url, use_proxy=False)
# Возвращает: "data:image/jpeg;base64,..."
```

### Без прокси!
**Важно**: Фото скачиваются БЕЗ прокси. Mercari CDN не блокирует прямые запросы.

---

## 📝 СТАТУС ПРОЕКТА

**Дата обновления**: 19 ноября 2024
**Статус**: ✅ PRODUCTION READY
**Готовность**: 95%+

### Что работает
- ✅ Поиск товаров через mercapi
- ✅ Загрузка HIGH-RES фото (без прокси)
- ✅ Сохранение в PostgreSQL с base64
- ✅ Фильтрация дубликатов
- ✅ Telegram уведомления с high-res фото
- ✅ Web UI для управления
- ✅ Hot-reload конфигурации

### Следующие шаги
1. Задеплоить на Railway
2. Проверить работу в production
3. Мониторить логи

---

## 💡 КОНТЕКСТ ПОСЛЕДНЕЙ СЕССИИ

**Задача**: Реализовать загрузку фото в полном разрешении

**Выполнено**:
1. Создан модуль `image_handler.py` для HIGH-RES фото
2. Исправлена интеграция с `mercapi` (query= вместо keyword=)
3. Обновлён `core.py` для загрузки и сохранения фото
4. Протестирован полный цикл: поиск → БД → Telegram
5. Очищен проект от ненужных файлов (43+ файла удалено)

**Результаты тестов**:
- 120 товаров найдено
- 70 новых добавлено с HIGH-RES фото
- 50 дубликатов отфильтровано
- 100% успешность загрузки фото

**Статус**: Готов к деплою на Railway

---

## 🔧 TROUBLESHOOTING

### Фото не скачиваются
- Проверить URL в логах (должен содержать `/item/detail/orig/photos/`)
- Убедиться что `use_proxy=False`

### mercapi ошибки
- Проверить синтаксис: `search(query=...)`  не `search(keyword=...)`
- Проверить атрибуты: `item.id_`, `item.name`, не `item.id`, `item.title`

### База данных
- `image_data` должен быть TEXT, не BYTEA
- Формат: `data:image/jpeg;base64,...`

---

*Этот файл - единственный источник документации для AI агентов.*
*Обновляется при значительных изменениях.*
